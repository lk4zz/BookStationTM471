model Rating {
    id     Int   @id @default(autoincrement())
    userId Int
    bookId Int
    value  Float

    ratedBook Books @relation(fields: [bookId], references: [id], onDelete: Cascade)
    rater     User  @relation(fields: [userId], references: [id], onDelete: Cascade)

    @@unique([userId, bookId])
}
