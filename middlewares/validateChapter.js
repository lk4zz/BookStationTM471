

const ValidateChapter (req, res, next) => {
    let { chapterNum, title, isLocked } = req.body;



}



deletechapter{

    deleted chapter done

    findMany 
    where: chaperNum > chapterNum of deleted chapter
}