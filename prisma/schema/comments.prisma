model Comments {
    id  Int @id @default(autoincrement())
    userId Int
    bookId Int
    commenter User @relation(fields: [userId], references: [id], onDelete: Cascade)
    commentOn Books @relation(fields: [bookId], references: [id], onDelete: Cascade)
    comment String

}